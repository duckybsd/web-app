(function(window, angular) {
    'use strict';

    angular.module('app.core')
        .controller('NavCtrl', NavCtrl);

    NavCtrl.$inject = [];

    function NavCtrl() {
    }

})(window, window.angular);
